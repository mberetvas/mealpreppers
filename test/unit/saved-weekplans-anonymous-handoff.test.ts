import { describe, it } from 'vitest'

/** SQLite integration coverage lives in `planning-sqlite-repository.test.ts`. */
describe('saved-weekplans anonymous handoff (moved)', () => {
  it('is covered by planning-sqlite-repository.test.ts', () => {})
})
